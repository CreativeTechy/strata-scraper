# Read by docker-compose.yml (via its automatic root .env lookup) to
# configure the `db` service. Keep these in sync with backend/.env's
# DATABASE_URL - that file connects to this same database over the Docker
# network (host "db") but is not itself interpolated by docker-compose.yml.
POSTGRES_DB=scraper
POSTGRES_USER=scraper
POSTGRES_PASSWORD=scraper
